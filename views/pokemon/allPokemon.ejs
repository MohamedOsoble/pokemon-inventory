<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title><%=title%></title>
</head>
<body>
    <%-include('../misc/nav')%>
    <h1><%=title%></h1>
    <ul class="pokemon-list">
        <% pokemonList.forEach((pokemon) => { %>
            <%- include('pokemonCard', {pokemon: pokemon}); %>
        <% }); %>
    </ul>
</body>
</html>